/*
 * desc: Write a JavaScript program to check whether a given fraction is proper or not. 
 * author: calvinmas@spacemilk
 */

const result = frac => frac > 1 ? 'im': '' + 'proper fraction';

console.log(result(3/4));
